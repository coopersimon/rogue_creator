func monster_action() {
    // if (player is next to monster) {
    //  attack player
    // else if (player is in room/within x tiles) {
    //  move toward player
    // else
    //  move in random direction
}

func monster_delete() {
    print(this.name + " has died!");
    delete(this);
}

func goblin_init() {
    this.exp = 5;
    this.hp = rand(3,4);
    this.att = 1;
    this.def = 1;
    this.wis = 1;
    this.dex = 1;
}
